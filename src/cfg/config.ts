const config_dir = '~/.config/gitlab-cli';
const config_file = config_dir + '/config.json';

const repo_cfg_dir = '.git/cli';
const repo_members = repo_cfg_dir + '/members';
const repo_issues = repo_cfg_dir + '/issues';
const repo_merge_requests = repo_cfg_dir + '/merge_requests';
const repo_branchs = repo_cfg_dir + '/branchs';
